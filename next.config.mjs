/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is a headless API, no UI pages needed.
  async headers() {
    return [
      {
        source: "/:path*",
        headers: [
          {
            key: "Cross-Origin-Opener-Policy",
            value: "same-origin-allow-popups",
          },
        ],
      },
    ];
  },
};

export default nextConfig;
